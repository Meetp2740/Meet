// const naviContainer = document.querySelector('.btn_effect');

// naviContainer.addEventListener('mousemove', (e) => {
//     const rect = naviContainer.getBoundingClientRect();
//     const x = e.clientX - rect.left - 75; // Adjusting for the center of the circle (80px / 2)
//     const y = e.clientY - rect.top - 75;  // Adjusting for the center of the circle (80px / 2)
//     naviContainer.style.setProperty('--circle-x', `${x}px`);
//     naviContainer.style.setProperty('--circle-y', `${y}px`);
// });

// naviContainer.addEventListener('mouseleave', () => {
//     naviContainer.style.setProperty('--circle-x', `0px`);
//     naviContainer.style.setProperty('--circle-y', `0px`);
// });